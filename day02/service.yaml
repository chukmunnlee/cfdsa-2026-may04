---
apiVersion: v1 
kind: Service 
metadata: 
  name: fortune-svc 
  namespace: myns
spec:
  type: ClusterIP
  selector: 
    name: fortune 
    env: prod
  ports:
  - port: 5000 # service port
    targetPort: 3000 # containerPort